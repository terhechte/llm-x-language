Please don't include a usage example in the code. Just the function
The name of the function should be `example`. E.g. `def example(input: {{string_type}}) -> {{string_type}}` with the parameters explained earlier
