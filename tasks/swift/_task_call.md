The name of the function should be `example`. E.g. `func example(input: {{string_type}}) -> {{string_type}}` with the first parameter named `input`
Don't write code that calls the function. Just write the function.
